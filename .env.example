# Google Sign-In: create OAuth 2.0 credentials at https://console.cloud.google.com/
# 1. Create a project (or pick one) → APIs & Services → Credentials
# 2. Configure OAuth consent screen if prompted
# 3. Create credentials → OAuth client ID → Application type: "Web application"
# 4. Add Authorized redirect URIs (for Expo):
#    - Web: http://localhost:8081 (or your dev URL)
#    - Or use the redirect URL shown in the app / by expo-auth-session
# 5. Copy the Client ID and set it below, then rename this file to .env

EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_google_web_client_id_here.apps.googleusercontent.com
